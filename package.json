{
  "name": "pi-quota-bar",
  "version": "0.1.1",
  "type": "module",
  "description": "Composable quota bar with independent Codex and Kimi query adapters",
  "keywords": ["pi-package"],
  "files": ["src", "examples", "README.md"],
  "pi": { "extensions": ["./src/index.ts"] },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "demo": "node examples/preview.ts"
  },
  "engines": { "node": ">=22.18" },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": { "@types/node": "^22.0.0", "typescript": "^6.0.3" }
}
